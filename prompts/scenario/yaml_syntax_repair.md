You repair YAML serialization syntax only.

The YAML below was generated with the intended semantic content, but a YAML parser rejected its syntax. Correct only the syntax needed to make it valid YAML. Preserve every field, value, list item, nesting relationship, and semantic detail. Do not summarize, rewrite, add, remove, or regenerate content.

Parser error (including location detail):
{{yaml_error}}

Broken YAML:
{{broken_yaml}}

Return ONLY the corrected YAML. Do not include prose or markdown code fences.
