---
topic: yt.flow PRD — Python/LangGraph SCP Content Pipeline
updated: 2026-06-30T22:35
---

- (decision) Langfuse self-host: homelab-gitops (existing k8s/GitOps infra)
- (decision) SQLite migration: standard Python ORM approach (no schema redesign)
- (decision) Interface: API only (FastAPI), no CLI for now
- (decision) FFmpeg: standard subprocess wrapper (orthodox approach)
- (decision) Prompt migration: migrate .tmpl files to Langfuse Prompt Hub first, before pipeline work
- (decision) Transition strategy: big-bang cutover, no hybrid/adapter layer
- (decision) Target user: solo (Jay only); future scope noted as generic video generation pipeline
- (decision) Working mode: Coaching path, Vision+Features entry (internal tool, single operator, pipeline-focused)
- (decision) Core success outcomes: (1) quality issues diagnosable and fixable quickly, (2) A/B prompt testing supported
- (decision) Pain: both blind spots confirmed — (1) cannot identify which stage caused quality failure, (2) prompts buried in code require redeploy to change
- (decision) A/B testing: automated evaluation (not manual comparison)
- (decision) Auto-eval mechanism: both LLM-as-judge AND rule-based metrics (balanced)
- (decision) Success metric: quality issue root cause identifiable within 30 minutes via Langfuse traces
- (decision) Pipeline equivalence: Go version has incomplete/buggy output; use Go impl as structural reference only, not output benchmark. Python rewrite sets the new quality bar.
- (decision) Image generation: ComfyUI running locally, integrated via HTTP API (not a cloud image model)
- (decision) Performance: max 2 hours per video, quality takes priority over speed
- (decision) Pipeline resume: required — resume from failed node; also support full restart as an option
- (decision) LLM provider: DeepSeek V4 (for scenario and image prompt generation)
- (decision) TTS provider: Qwen TTS (latest version)
- (decision) FR-1.5 subtitle method: forced alignment — script text already known from scenario stage, sync timing against TTS audio output
- (decision) Qwen TTS: cloud API, not a throughput bottleneck — 2hr limit is dominated by ComfyUI image generation time, not TTS API calls
- (event) PRD finalized — status set to final
- (decision) Web UI added to scope: React SPA, FastAPI-served, with stage approval gates, artifact preview, real-time progress, stage retry, A/B comparison view
- (decision) Prompt editing: stays in Langfuse UI, not in the React app
- (decision) Stage approval gate: pipeline pauses at each stage completion; user approves or rejects before proceeding — changes pipeline from fully automated to human-in-the-loop
- (decision) UI design: use frontend-design plugin with Claude AI capabilities for component/layout design throughout F7 development
- (event) PRD updated: F7 Web UI added (React SPA, stage gates, artifact preview, A/B comparison, SSE); FR-9 gate behavior added to F1; FR-30~34 API endpoints added; re-set status to draft
- (event) PRD re-finalized after Web UI addition — FR-1~42, 8 OQs, status: final
- (event) PRD updated from architecture session: added FR-33 (GET /scps), FR-34 (PATCH artifact), FR-44 (inline text editor); renumbered F7 FRs to 37-44; updated F6 FRs to reflect SQLModel+LangGraph SqliteSaver decision; closed OQ-3 and OQ-7.
