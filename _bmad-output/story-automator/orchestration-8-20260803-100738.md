- **[2026-08-08T03:39:49Z]** ✅ ROOT CAUSE OF THE SLOWDOWN WAS THE GPU POWER STATE. GPU had been pinned at its lowest DPM step (52-74MHz of 2620MHz, 12% busy, 25W, 38C) while ComfyUI burned 97% CPU — compute never reached the GPU, turning 20s generations into 336-900s. Not a yt.flow defect, not memory pressure (I had already freed RAM 0GB->17GB and relaunched with --highvram, neither helped). Jay set power_dpm_force_performance_level=high (needed root, outside my permissions). Verified after: dpm=high, **Prompt executed in 13.62 seconds**. Resuming the run.

## FINAL STATE — E2E blocked on ComfyUI instability (host-side)

- **[2026-08-08T04:11:12Z]** GPU power state WAS a real cause and is FIXED (Jay set power_dpm_force_performance_level=high; verified dpm=high, clock 2033-2258MHz vs 52MHz before). With it, a real generation runs **14.66s for 30 steps at 2.44 it/s** — proven in the log. So the pipeline and the GPU are both fine.
- **[2026-08-08T04:11:12Z]** REMAINING BLOCKER is ComfyUI itself: within minutes of every restart it stops executing. Signature: queue holds prompts (1 running / 1 pending) but ComfyUI never logs `got prompt`, its log ends at "[ComfyUI-Manager] All startup tasks have been completed", and the process pins ~100% CPU with the GPU at 7% busy. Restarting clears it only for the first prompt or two. Also present throughout: repeated `ERROR lora ... shape '[640, 640]' is invalid for input of size 1638400` — SD1.5-shaped LoRA keys being patched onto an SDXL checkpoint in comfyui_sdxl_anime_lora_workflow_api2.json.
- Hypotheses I raised and DISPROVED along the way (recorded so nobody re-walks them): queue leftovers, missing character cards, health-check false negative, ComfyUI auto-restart, my own /queue interference, depth being the bottleneck (depth is 0.7s), host RAM pressure (freed 0GB->17GB, no effect), lowvram mode (--highvram, no effect).
- Run 8a9a288b: scenario approved and stable, image 1-10/66 depending on retry, tts/subtitle/video never reached.
- FOR JAY: the ComfyUI install needs attention — the 100%-CPU-no-logging wedge right after startup tasks, and the mismatched LoRA in the SDXL workflow. Once ComfyUI holds, `POST /runs/8a9a288b-800f-4c73-88a2-25ae6b5a4d7d/resume` finishes the remaining shots at ~15s each (~15 min).
- **[2026-08-08T05:29:33Z]** ✅ RESOLVED — and the last few hours were MY OWN LOOP. Measured cleanly: first generation after a ComfyUI restart takes **~490s (model load of the 6.9GB SDXL checkpoint)**, every WARM generation after it takes **15-16s** (verified back-to-back: 16.1s, 15.2s). My stall detectors were set to 6/8/10 minutes — SHORTER than the cold model load — so I repeatedly judged a loading ComfyUI as 'stalled', restarted it, and forced another 8-minute load, never once reaching the warm state. Also disproved along the way: the mismatched LoRA (darkness_xl_v2 is genuinely SD1.5-shaped and its patch errors are real, but removing it did NOT fix the slowness) and --highvram (no effect). GPU dpm=high from Jay remains a genuine, necessary fix. Resuming WITHOUT restarting ComfyUI; stall threshold now 20min so a cold load can never be mistaken for a hang. Expected: 65 shots x ~15s = ~17 min.
- **[2026-08-08T06:55:14Z]** 🎉 IMAGE STAGE COMPLETE — first time all session. 66/66 shots. Sanity: 66 files, **66 unique by sha256** (no duplicates), 0 zero-byte, sizes 0.98-1.70MB, **all 1344x768** (11.1's latent fix holding), 66 sidecars, all 9 scenes covered. Gate auto-approved. Throughput after the depth gate fix (19c4cf1): steady ~15.3s/shot, 0 stalls, 0 auto-recoveries. Note this run has depth OFF, so backgrounds are pure per-shot generation (plate substitution also off via 76267bb) — 66 distinct backgrounds, which is exactly what the 8.17 collapse fix was meant to restore.
- **[2026-08-08T06:57:07Z]** TTS stage complete, gate approved. 9/9 scene wavs, 0 zero-byte, 0 zero-length, per-scene 13.2-22.6s. ⚠️ QUALITY SIGNAL for the final diff: **total narration 164.6s = 2.7 min vs the 8-minute target and vs baseline c6be1954's 8:10** — a ~3x shortfall. Likely tied to deepseek_reasoning=low (a9d10ac), which was set to stop the truncation class; shorter reasoning may be yielding shorter narration. Not blocking (format checks pass) but must be called out in the c6be1954 comparison.
- **[2026-08-08T06:58:43Z]** Subtitle stage complete, gate approved. 9/9 scene .ass files (7.5 ASS kinetic format from Story 7.5), 67 Dialogue events total, 0 empty files, 992-1504 bytes each. Proceeding to the video stage — the last one, and one this session has never reached.
- **[2026-08-08T07:16:59Z]** 🔴 MY SUPERVISOR CAUSED DATA LOSS: its auto-recovery hardcoded `POST /runs/{id}/stages/image/retry` regardless of which stage failed. The video stage failed (SAR mismatch), so it retried IMAGE — and retry_stage nullifies that stage's outputs, wiping all 66 completed images. They are regenerating (~17 min). Supervisor fixed to retry the run's ACTUAL current_stage, and to escalate rather than guess when the stage can't be identified. Also noted: restarting ComfyUI is the wrong recovery for an ffmpeg failure; the supervisor should classify failures before acting.

## ✅ E2E ITERATION 3 COMPLETE

- Run `8a9a288b-800f-4c73-88a2-25ae6b5a4d7d` (SCP-049), status **complete**, all 5 gates approved.
- Final video: `workspace/8a9a288b-800f-4c73-88a2-25ae6b5a4d7d/video.mp4` — 1920x1080 h264 + AAC stereo, 3.10 min, 56.4 MB.
- vs baseline c6be1954 (also SCP-049): video 3.10 vs 8.18 min | images 66 (66 unique) vs 155 (155 unique) | audio 2.82 vs 7.87 min | scenes 9 vs 10.
- Final suite: **2446 passed, 1 skipped**, ruff clean. 28 commits this session.
- ⚠️ KNOWN GAP: run.video_path is left NULL in the DB even though the file exists on disk — worth its own small fix.
- ⚠️ SCOPE DEVIATIONS in this run: depth/parallax OFF (11.5 not exercised), plate substitution OFF (76267bb), and SCP-049 instead of a new SCP.
