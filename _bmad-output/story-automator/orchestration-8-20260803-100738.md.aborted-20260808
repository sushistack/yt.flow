---
# Orchestration State Document
epic: "8"
epicName: "yt.flow Epic 8 + Epic 11 quality subset (8.19, 11.5, 8.20)"
storyRange: ["8.19", "11.5", "8.20"]
status: "ABORTED"
currentStory: "8.20"
currentStep: "e2e-in-flight-watched"
stepsCompleted: []
lastUpdated: "2026-08-08T00:00:00Z"
createdAt: "2026-08-03T10:07:38Z"

# Configuration
aiCommand: "claude --dangerously-skip-permissions"
overrides:
  skipAutomate: true
  maxParallel: 1
customInstructions: "Include the research doc (_bmad-output/planning-artifacts/research/technical-yt-flow-quality-strategy-research-2026-08-01.md) in EVERY story context (create-story and dev-story sessions must read it). ComfyUI (:8188) live validation is ALLOWED. Story files already exist as ready-for-dev on disk; refresh context from epics.md draft where thin. Gates: auto-approve when an artifact sanity check passes (file exists, shot count, audio duration, card alpha). PER-STORY: 8-19 must begin at epics.md Stage 0 (diagnose 3-5 real mismatch cases with measurements); if the root cause is NOT \"STOCK reuse decision failure\", STOP with no code changes and report; Stage 1 only on proven cause; Stage 2 (embeddings) FORBIDDEN. 11-5: if DepthFlow is blocked (license/GPU/OpenGL), switch to the python fallback path named in epics.md. 8-20: Qwen-Image-Edit-2511 GGUF based per the epics.md re-judgment; IPAdapter FaceID/InstantID/PuLID are FORBIDDEN. 11-6 MUST NOT be started. After all three are done: E2E Iteration 3 (full pipeline on one new SCP), then report final video path + diff vs run c6be1954 and stop."
agentsFile: "_bmad-output/story-automator/agents/agents-orchestration-8-20260803-100738.md"
complexityFile: "_bmad-output/story-automator/complexity-orchestration-8-20260803-100738.json"
policyVersion: 1
policySnapshotFile: "_bmad-output/story-automator/policy-snapshots/20260803-100738-023433cb.json"
policySnapshotHash: "023433cb"
legacyPolicy: false

# Agent Configuration (v3.0.0)
agentConfig:
  defaultPrimary: "auto"
  defaultFallback: false

# Session Tracking
activeSessions: []
completedSessions: []
---

# Orchestration Log: yt.flow Epic 8 + Epic 11 quality subset (8.19, 11.5, 8.20)

## Configuration

**Epic:** 8
**Story Range:** 8.19, 11.5, 8.20
**Created:** 2026-08-03T10:07:38Z

**Overrides:**
- Skip Automate: true
- Max Parallel: 1

**Custom Instructions:**
Include the research doc (_bmad-output/planning-artifacts/research/technical-yt-flow-quality-strategy-research-2026-08-01.md) in EVERY story context (create-story and dev-story sessions must read it). ComfyUI (:8188) live validation is ALLOWED. Story files already exist as ready-for-dev on disk; refresh context from epics.md draft where thin. Gates: auto-approve when an artifact sanity check passes (file exists, shot count, audio duration, card alpha). PER-STORY: 8-19 must begin at epics.md Stage 0 (diagnose 3-5 real mismatch cases with measurements); if the root cause is NOT "STOCK reuse decision failure", STOP with no code changes and report; Stage 1 only on proven cause; Stage 2 (embeddings) FORBIDDEN. 11-5: if DepthFlow is blocked (license/GPU/OpenGL), switch to the python fallback path named in epics.md. 8-20: Qwen-Image-Edit-2511 GGUF based per the epics.md re-judgment; IPAdapter FaceID/InstantID/PuLID are FORBIDDEN. 11-6 MUST NOT be started. After all three are done: E2E Iteration 3 (full pipeline on one new SCP), then report final video path + diff vs run c6be1954 and stop.

---

## Story Progress

| Story | create-story | dev-story | automate | code-review | git-commit | Status |
|-------|--------------|-----------|----------|-------------|------------|--------|
| 8.19 | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | pending |
| 11.5 | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | pending |
| 8.20 | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | pending |

---

## Action Log

<!-- Timestamped action entries will be appended here -->

---

## Session References

| Session ID | Story | Step | Status | Started | Completed |
|------------|-------|------|--------|---------|-----------|
<!-- Session entries will be appended here -->

---

## Pending Decisions

<!-- Escalations awaiting user input will be listed here -->

---

## Learnings & Recommendations

<!-- Populated during wrapup phase -->
- **[2026-08-03T10:08:45Z]** Starting story 8.19 (create-story pre-verified: story file exists)
- **[2026-08-03T10:20:35Z]** ENV PROBE (orchestrator, read-only): ComfyUI :8188 was DOWN at run start (curl 000). Launched /home/jay/workspaces/ComfyUI/run.sh in background; readiness watch armed. Needed for 8.20 live renders and E2E Iteration 3.
- **[2026-08-03T10:20:35Z]** ENV PROBE: 11.5 DepthFlow prerequisites ABSENT — depthflow/moderngl/PyOpenGL all not installed, no refs in pyproject.toml or uv.lock. torch+transformers present. Signal only; the 11.5 dev session makes the DepthFlow-vs-python-fallback call per epics.md.
- **[2026-08-03T10:31:56Z]** ComfyUI :8188 now READY (HTTP 200). First launch failed: run.sh uses relative `venv/bin/activate` + `main.py`, so it must be started with cwd=/home/jay/workspaces/ComfyUI. Relaunched correctly. Env: ComfyUI 0.12.3, torch 2.11.0.dev+rocm7.1, AMD RX 9060 XT, vram_total 17.10GB / free 16.52GB. Relevant to 8.20 GGUF fit and E2E Iteration 3.
- **[2026-08-03T10:31:56Z]** ⚠️ CORRECTION to story 8-19 context: the story file asserts "yt_flow.db has no run rows and workspace/ contains no per-run artifacts" and concludes Stage 0 is impossible in this checkout. That premise is FALSE — yt_flow.db has 172 `run` rows and workspace/ has 260 per-run directories. The dev session did not bail on the false premise; it built real diagnosed cases and proceeded into Stage 1. Story file text should be corrected during review.
- **[2026-08-03T10:51:01Z]** 8.19 dev COMPLETE (sprint-status=review, uncommitted). Gates: 1673 passed/1 skipped, coverage 92.25%, ruff clean. Task 0 verdict SPLIT: cast=SUPPORTED (4 observed frames), location=NOT SUPPORTED (approved plates created 2026-08-02 but latest run is 07-12; 0 of 1843 run images byte-identical to a plate -> STOCK location reuse has never executed; key-selection accuracy 131/132). Stage 1 implemented for cast only: controlled role-description catalog in cast_decision.md (seeded to production) + deterministic guard _suppress_cast_on_no_figure_framing in build_scenes. AC5 text matcher deliberately NOT built (0 typo/hallucinated keys -> gating condition false). Embeddings/service/config/dependency/image.py changes: ZERO.
- **[2026-08-03T10:51:01Z]** 🚨 E2E BLOCKER surfaced by 8.19 (not fixed there, AC8/13 forbid image.py): 8.17's plate substitution at image.py:325-337 copies the plate and `continue`s, so image_prompt never renders and variant is chosen per-scene. Replaying c6be1954 against the current table collapses distinct backgrounds 132 -> 34 (85% of shots; scene5 containment-chamber 21 shots -> 1 image). Must be resolved before E2E Iteration 3 or the run's backgrounds will be degenerate.
- **[2026-08-03T11:14:45Z]** 8.19 code-review cycle 1 PASSED (verified: sprint-status=done). 8 findings, all fixed (5 MED / 2 LOW / 1 process). 1675 passed, coverage 92.27%, ruff clean. Review confirmed: hard constraints honored by diff (pyproject/uv.lock/config.py/image.py/services all unchanged, no asset_retrieval_service.py); AC5 non-implementation justified across all 134 runs with scenes (0 out-of-vocab location_key/card_key); prompt seeded as scenario/cast_decision v12 labels [production,latest]. Fixed the false-premise text still present at story line 38. Found+fixed a missing ordering test: swapping the guard vs _enforce_cast_diversity left all 293 tests green.
- **[2026-08-03T11:14:45Z]** 🚨 E2E BLOCKER CONFIRMED AND WORSE than 8.19 dev reported: the 132->34 figure counted (scene,key) selection events. Recomputing _plate_variant_index against the 42 plates on disk, the 132 keyed shots collapse to 18 distinct images (run-wide 155 -> 41). Per-shot variant selection caps at 27, so the real fix is that image_prompt is discarded entirely. Needs its own Epic 8 story; NOT fixable inside 8.19 (AC8/AC13).

## STOPPED — awaiting Jay's decision

- **[2026-08-03T11:15:33Z]** Orchestrator STOPPED by explicit user action: Jay denied the step-03b-execute-finish (git commit) tool call with "STOP what you are doing and wait for the user to tell you how to proceed."
- Progress at stop: 8.19 dev DONE + code-review PASSED (sprint-status=done), **NOT COMMITTED**. 11.5 and 8.20 not started. E2E Iteration 3 not started.
- Uncommitted working tree: prompts/scenario/cast_decision.md, src/yt_flow/domain/state.py, src/yt_flow/pipeline/nodes/scenario_chain.py, tests/pipeline/nodes/test_scenario_chain.py, plus story file + sprint-status.yaml.
- Open decisions for Jay: (1) commit 8.19 and continue to 11.5? (2) the confirmed 8.17 plate-substitution regression (132 keyed shots -> 18 distinct images, run-wide 155 -> 41) blocks E2E Iteration 3 and needs its own story first. (3) 11.5 DepthFlow prerequisites absent -> python fallback likely.
- Resume with: story-automator resume, state file _bmad-output/story-automator/orchestration-8-20260803-100738.md
- **[2026-08-03T12:19:47Z]** RESUMED by Jay: "커밋 남기고 계속 끝까지 진행해" — commit 8.19, then 11.5, then 8.20, then E2E Iteration 3.
- **[2026-08-03T12:20:01Z]** Story 8.19: ✅ COMPLETE — commit 3393a2c, sprint-status verified done.
- **[2026-08-03T12:20:30Z]** Starting story 11.5 (create-story pre-verified)
- **[2026-08-03T12:32:17Z]** 11.5 dev asked 2 interactive questions; orchestrator DECIDED autonomously per the stop-hook matrix (never block on user):
  - Q1 renderer ladder -> "depth warp in the fallback": ladder = depthflow (external runtime, OFF by default) / depth-warp (numpy+PIL, real 2.5D, works now) / legacy zoompan. No new dependency. This IS Jay's instructed python-fallback path since DepthFlow is unavailable.
  - Q2 depth model -> "swap to Depth-Anything Small (Apache-2.0), mark regeneration required". Reason: the session found a LIVE LICENSE VIOLATION — comfyui_depth_anything_v2_api.json uses depth_anything_v2_vitl.pth (Large, CC-BY-NC-4.0 non-commercial) and depth_placement_enabled=True is already shipping it. AC3 forbids exactly this. Also the depth cache key was image-sha256 only, so swapping models would keep serving stale maps; model identity now enters the key.
- **[2026-08-03T12:32:17Z]** Session also invalidated an AC1 premise: 8.17 did NOT leave depth maps missing — Story 8.16 already built a content-addressed depth cache and all 42/42 approved plates have depth maps. Non-destructive backfill was already effectively done.
- **[2026-08-03T12:32:17Z]** ComfyUI died mid-run (killed when its parent background task was reaped) and is RELAUNCHED DETACHED via setsid; verified 200. Informed the 11.5 session so its Task 6 live gate + 42-plate Small-model backfill run live instead of being recorded as deferred. ⚠️ 8.16 ground_line constants were measured against Large -> need re-measurement against Small.
- **[2026-08-03T14:17:27Z]** 11.5 dev COMPLETE (sprint-status=review, uncommitted). DoD 23/23, 1818 passed/1 skipped, ruff clean, File List == git status.
  - Renderer ladder built as decided: depthflow (rung 1, depthflow_enabled=false, unverified upstream API, exit 3 -> unavailable -> graceful demote) / depth-warp numpy+PIL 2.5D (rung 2, ACTIVE) / legacy zoompan (rung 3). No new dependency.
  - LICENSE DEFECT FIXED (found live, in the monetized path): depth workflow ran depth_anything_v2_vitl.pth (Large, CC-BY-NC-4.0) and the cache key was plate-bytes-only, so a model swap would serve Large maps forever. Now: vits (Apache-2.0) pinned in config + per-call injection + estimator contract in the cache key + RAISES (not warns) on non-commercial/unknown checkpoints.
  - services/depth_service.py deliberately NOT created — 8.16's compositing_service already owns the depth contract; creating it would be the duplication Dev Notes itself warned about.
  - LIVE GATES (RX 9060 XT, ComfyUI 0.12.3): Small 42/42 backfill OK (re-run = 0 inference, assets/ untouched); depth differential near 34px / far 23px @ 38px budget; border exposure 0/125 frames; card layer ratios 0.65/0.76/0.76 in the 0.60-0.80 band; all 3 fallback drills logged+traced; cache hit 0.01s, re-render byte-identical; warp 250 -> 53 ms/frame, E2E 71 ms/frame (~14 min for an 8-min video). AC4 proved numeric-sampler parity with 11.3's ffmpeg expression to 1e-12.
  - Regression recorded, NOT fixed (8.16 scope): Small produces flatter maps -> readable ground floor 41 -> 30 of 42; 12 plates now use _DEFAULT_GROUND fallback (far<=mid<=near ordering holds 30/30). _DEFAULT_GROUND re-measured against the in-use model. Side effect: motion-safe box now reserves the layer ceiling (46.08px/side) -> CHAR_MAX_H 796.34 -> 743.28, _CARD_HEIGHT_FRAC changed with it.
  - Jay-only items: DepthFlow rung-1 spike (uninstalled); video viewing verdict at ~/ytflow-11-5-review/ (A/B/C/D variants + 4 frames).
- **[2026-08-03T14:45:47Z]** 11.5 code-review cycle 1 PASSED (verified done). 7 fixed / 4 recorded. Suite 1826 passed/1 skipped (1818 + 8 new), ruff clean.
  - 🔴 HIGH FOUND+FIXED (would have degraded E2E Iteration 3): depth field misaligned against the plate on EVERY free background. _load_overscan_source cover-crops the plate but depth_gain_field plain-resized the depth map; these agree only for exactly-16:9 sources. Stock plates are 1920x1080 so it was invisible there, but shot backgrounds generate at 1216x832 -> 27px misalignment at the 40% row, ~100px at frame edges, foreground pixels driven by far-plane gain, disocclusion dilation on the wrong boundary. Every offline fixture is 1920x1080 so no test could see it. Fixed via a shared _cover_resize + DEPTH_EDGE_VERSION 1->2 to invalidate affected clips + size-parameterised test.
  - MEDIUM x5 fixed: _GROUND_Y_MAX still reserved 7.3's 12px while cards gained a 46.08px layer term (analytic worst case 34.1px past the bottom edge; observed trajectories survived by ~5px of coincidence = latent); contact shadow didn't ride the layer translation (detached up to 30.7px); warp_frame was tested but the production loop inlined a copy; depth_hit trace reported hits for re-inferred maps; four "byte-identical rollback" claims were FALSE (card box, _CARD_HEIGHT_FRAC, _DEFAULT_GROUND all moved regardless of the switch). LOW: DEPTHFLOW_RUNNER was CWD-relative.
  - Independent re-verification of the dev agent's two self-flagged areas: MotionSource seam is genuine (all 4 branches build from motion.bg_input/bg_chain; on a 2.5D clip bg_chain=="null", camera_shake=="", parallax_enabled False) and warp math is correct (synthetic half/half depth markers: near +38.0px vs 38.4px budget, far +10.0px vs 9.6px expected). Benchmark was UNDERSTATED not inflated: measured 45.1 ms/frame vs 53 claimed, 59 ms E2E vs 71 claimed.
  - Recorded not fixed: rung 2 has no timeout; the "0 black-border frames" evidence is unfalsifiable because _warp_block clamps coordinates (overscan math independently sound); xy_gain compresses base pan to 1.16% of width at a 3% budget on high-trauma archetypes; 42 orphaned Large depth maps left for Jay to delete.
- **[2026-08-03T14:45:47Z]** Story 11.5: ✅ COMPLETE — commit 2f5153e, sprint-status verified done.
- **[2026-08-03T14:46:14Z]** Starting story 8.20 (create-story pre-verified)
- **[2026-08-03T15:06:09Z]** 8.20 dev Task 1 spike results (session still running; Q4_K_M GGUF 13.24GB was ~19% downloaded, ~40min remaining, session continues to live measurement on completion):
  - VNCCS REJECTED with evidence (pinned rev 1bb732eb, MIT). Good news: grep for faceid|insightface|instantid|pulid|antelopev2|buffalo_l returned 0 hits, so AC7's ban premise holds. But the story's core premise is DISPROVEN — all 5 VNCCS workflows are 2-5 node shells wrapping giant VNCCS_ControlCenter/VNCCS_PoseStudio nodes; graph construction lives in 21,412 LOC of python calling nodes.common_ksampler directly. There is NO extractable API graph. VNCCS's Qwen use is Qwen2.5-VL-7B as a CAPTIONER, not a generator; main path is Illustrious/Anima SDXL, auto-downloads from HF at node runtime and needs llama-cpp-python — exactly what AC2 forbids. -> native Qwen-Image-Edit-2511 adopted, per Jay's instruction.
  - AC6 got SIMPLER than the story assumed: TextEncodeQwenImageEditPlus natively accepts image1/image2/image3, so the structure guide is just a second reference image (OpenPose raster for humanoid, scribble for non-humanoid) — ONE path, different guide raster. ControlNet is not used at all, which dissolves the "Edit-2511 x ControlNet Union compatibility unverified" risk.
  - 🆕 DEFECT FOUND: non-humanoid reference cards are broken. SCP-682 and SCP-1471 standing_front are status=approved but the actual files are 1664x928 OPAQUE RGB with no alpha channel at all. The 6 humanoid cards are correct (832x1216 RGBA). AC7 needs an approved card as identity source and AC10 needs 832x1216 RGBA, so non-humanoid AC6/AC14 cannot satisfy card invariants without regenerating the base cards.
  - Budget lever secured: lightx2v/Qwen-Image-Edit-2511-Lightning 4-step LoRA (Apache-2.0, 850MB) vs the official 40-step/cfg4 default.
  - Env (all Apache-2.0, hashes recorded): ComfyUI-GGUF 6ea2651e + gguf 0.19.0 installed, GGUF nodes load. ComfyUI was restarted by the session (GGUF nodes register only at startup) with an empty queue.
- **[2026-08-03T15:06:09Z]** ⚠️ 8.20 IS DEPENDENCY-BLOCKED, and the block is real: Story 13.3 is ready-for-dev and the `ytflow:*` title resolver does not exist in src/. 8.20's own AC3/AC9 require 13.3's resolver + env snapshot hashes, so building it inside 8.20 would create the second resolver the story explicitly forbids. Task 7 additionally requires Jay's visual approval, impossible unattended.
- **[2026-08-03T15:06:09Z]** ORCHESTRATOR SCOPE DECISION: I am NOT inserting Story 13.3. Jay's authorized scope was exactly 8-19 -> 11-5 -> 8-20 + E2E, and he was explicit about scope boundaries (11-6 forbidden). 13.3 is a separate story and starting it is beyond the ask. 8.20 will deliver its Task 1 outputs (minimal workflow JSON + README + measured decision record) and be reported as PARTIAL / blocked-on-13.3 rather than forced to done.
- **[2026-08-03T15:06:09Z]** NOTE: unsent text has been appearing in the child session input lines ("commit this", "run code review", "13.3 먼저 하고 8.20 이어가자"). Not sent by the orchestrator and NOT treated as user instruction. Deliberately not pressing Enter in that session and not clearing the line (it may be Jay's own typing).
- **[2026-08-04T11:28:05Z]** 8.20 dev session LOST (tmux not_found; sprint-status still ready-for-dev, never advanced). Collateral: the GGUF download was a child of that session and died at 6.23GB of 13.24GB, and the ComfyUI instance the session had restarted died with it. Surviving deliverables (untracked, real): data/workflows/comfyui_qwen_pose_edit_api.json (3292B) + data/workflows/README-qwen-pose-edit.md (7518B). Retrying dev as attempt 2/5 with instructions to resume rather than restart the download.
- **[2026-08-04T11:38:32Z]** 8.20 attempt 2 asked how to handle the 13.3 gate; orchestrator DECIDED: option 3 = "Task 1 + Task 2, then stop". Rationale: option 2 (implement 13.3 first) is a 2-story scope expansion Jay did not authorize; option 1 (spike only) leaves authorized work on the table. Option 3 implements everything in 8.20 that does NOT depend on 13.3 — Character.pose_conditioning column + migration + backfill, pose_guide_key cast contract, pose_guides catalog — while leaving only AC9's env-snapshot-hash item waiting on 13.3 and creating NO second ytflow:* resolver. ComfyUI back up (200); GGUF download resumed and progressing (6.23 -> 7.92GB).
- **[2026-08-04T13:01:32Z]** 8.20 dev attempt 2 finished its authorized scope (Task 1 + Task 2) and is committed as 7d2f831. Status left **in-progress**, NOT review/done — correctly, since Tasks 3-7 are unstarted and DoD is unmet.
- **[2026-08-04T13:01:32Z]** 🔴 8.20's PRIMARY TECHNIQUE WAS EMPIRICALLY REJECTED by the live gate. Two independent blockers, 5 ComfyUI runs on RX 9060 XT:
  (1) The guide is injected as CONTENT, not conditioning. TextEncodeQwenImageEditPlus image2 is a reference image, not a ControlNet hint, so Qwen-Edit literally draws the guide into the frame (COCO-18 skeleton -> an actual skeleton lying in the shot; creature silhouette -> a white bear overlapping SCP-682). Both schemas fail identically, so it's a property of the technique, not bad guide art. Decisive: the no-guide edit_only baseline ALREADY achieved the requested action from text alone -> zero evidence the guide contributed structure. Per AC6 the session refused to report reference-only as conditioning success.
  (2) It does not fit in 16GB. 2 of 5 runs CUDA OOM; peak 15.20-16.18GB against 15.92GB available. The OOM point is NOT diffusion — it's InspyrenetRembg: the 13.24GB GGUF stays resident (11.85GiB) and the cutout needs 4.50GiB more, and that node exposes no device/resolution knob. AC2's "cutout inside the workflow" is structurally impossible at this model size. Q5 auto-excluded.
  Identity preservation WAS excellent (049 mask/hood/coat, 682 reptilian body all survived), so the story's rejection of FaceID/InstantID/PuLID was the right call. Evidence: _bmad-output/implementation-artifacts/8-20-live-validation/ (DECISION-RECORD.md, measurements.jsonl, 3 rendered failure PNGs).
- **[2026-08-04T13:01:32Z]** 8.20 Task 2 DELIVERED: pose_conditioning column + idempotent migration (validated against a real pre-8.20 table) + backfill applied to the real DB (7 changed, re-run 0) + closed pose_guide_key cast field + 6 guides registered/approved in the existing AssetService manifest + AC9 fingerprint now requires env_snapshot_sha256 (forces 13.3 wiring). Guides derived from real demand (8 pose_hint kinds mined from 2075 checkpoints); 3 head/gaze-only kinds deliberately excluded; VNCCS's 12 posesets unusable as guide sources (all standing character sheets, directly violating AC5's ban on restating existing poses) so only limb proportions were borrowed.
- **[2026-08-04T13:01:32Z]** Orchestrator verification of 8.20's test claim: dev session reported "1936 passed / 0 failed". My independent run got **1935 passed / 1 skipped / 1 FAILED** — tests/api/test_e2e_stub_run.py, the known pre-existing _drain_bg_tasks 10s timeout (load-dependent, unrelated to 8.20, one-line fix already handed off). Ruff clean. Recorded honestly in the commit message rather than repeating the session's claim.
- **[2026-08-04T13:01:32Z]** 8.20 code-review INTENTIONALLY NOT RUN: the review loop's exit condition is sprint-status=done, which 8.20 cannot truthfully reach. Running it would burn 5 cycles and escalate. Deferred until the technique decision is made.
- **[2026-08-04T13:06:43Z]** Jay's decision on the E2E blocker: **gate plate substitution off, then run E2E**. Spawning a bounded fix session (orchestrator does not edit source directly). Scope: one config flag + one guard so image_prompt renders again; NOT the full plate-vs-prompt reconciliation story.
- **[2026-08-04T13:26:17Z]** E2E blocker FIXED and committed 76267bb: stock_plate_substitution_enabled (default False) gates 8.17's substitution; flag-on is byte-identical, flag-off restores image_prompt rendering. 1938 passed/1 skipped, ruff clean. Root fix (per-shot, prompt-aware plate reuse) remains a separate story.
- **[2026-08-04T13:38:17Z]** E2E Iteration 3 STARTED. SCP-999 "The Tickle Monster" (Safe) — chosen as the new SCP: only SCP-682 and SCP-999 were unused (172 prior runs cover 049/096/173/1471), and 682 was rejected because 8.20 confirmed its standing_front card is a broken 1664x928 opaque RGB with no alpha, which would confound evaluation of 11.5/8.19. Run id: caf09fc8-b7a4-4dc9-a348-f28d8ea70695. Preconditions: API up on :8000, ComfyUI clean restart with 16.52GB/17.10GB VRAM free, stock_plate_substitution_enabled=False (so image_prompt renders and backgrounds are generated per shot).
- **[2026-08-04T13:38:17Z]** Note: a wedged ComfyUI instance (started by the 8.20 session 2h08m earlier, 68% CPU / ~19GB RSS, holding :8188 without answering HTTP even at 20s) had to be SIGKILLed before a clean instance could bind.
- **[2026-08-04T14:08:50Z]** E2E Iteration 3 attempt 1 (run caf09fc8) FAILED, then attempt 2 (run 2f362fac) diagnosed. Root cause is NOT a ComfyUI hang: a clean cold generation on this host completes in ~400s (verified: Inspyrenet Rembg 100%, history=1), but comfyui_client's poll budget is a hardcoded max_polls=180 x poll_interval=1.0s = 180s. The client gives up at 180s, the caller retries, each retry adds another prompt to the queue -> queue grew 3->6 while history stayed 0, and every subsequent call timed out worse. Verified NO call site overrides max_polls (character_image_provider, compositing_service, composite_harmonization, image.py all use the default), so the entire pipeline is capped at 180s per generation on a host that needs ~400s cold. Character-card generation for SCP-999 therefore never succeeded. Spawning a bounded fix session to make the poll budget configurable and raise the default.
- **[2026-08-04T14:33:51Z]** ComfyUI poll-budget defect FIXED and committed 80b5a8c: comfyui_generation_timeout_sec (900s default) + comfyui_poll_interval_sec, resolved via _poll_budget() from Settings; all 4 call sites inherit it positionally. 1939 passed/1 skipped, ruff clean. This unblocks E2E Iteration 3 on this host.
- **[2026-08-04T14:34:45Z]** E2E Iteration 3 attempt 3 STARTED with all fixes live (plate substitution gated off 76267bb + 900s poll budget 80b5a8c). SCP-999, run id 118d5c37-1d83-4ceb-8e0e-d2f536732e4d. Clean ComfyUI (16.53GB free) + clean API.
- **[2026-08-04T14:43:47Z]** 🔴 SCP-999 IS HARD-BLOCKED: scenario/structure truncates at finish_reason=length, completion_tokens=16384, **3 out of 3 attempts** (runs 2f362fac, 118d5c37, ae0f6735) — deterministic, not variance. YTFLOW_DEEPSEEK_MAX_TOKENS is already 16384 in .env (the Story-6.9 conclusion is that a 16k truncation means runaway generation, not an insufficient budget, so raising it is the wrong lever). Story 6.9's TruncationError -> full-rewrite fallback covers the *repair* path, not the initial structure generation, so the run just fails. NEW DEFECT also found: the truncation dump writes a **0-byte** file (tmp/truncations/scenario_structure-1785854568693650298.txt) despite logging "full runaway raw -> <path>", destroying the diagnostic evidence. Both deserve a story.
- **[2026-08-04T14:43:47Z]** ORCHESTRATOR DECISION — deviating from Jay's "new SCP" instruction, deliberately and reported: SCP-999 cannot complete; SCP-682 was the only other unused SCP and 8.20 confirmed its standing_front is a 1664x928 opaque RGB with no alpha, which would composite full-frame opaque cards (the iteration-0 D13 critical) and defeat the point of a quality-evaluation run. Running **SCP-049** instead — it has good cards AND is the SCP of baseline run c6be1954, which makes the requested diff apples-to-apples rather than cross-SCP. Jay's "new SCP" requirement is NOT met and needs his call once SCP-999's runaway is fixed.
- **[2026-08-04T14:43:47Z]** Correction to an earlier orchestrator note: the "Langfuse initialized without public_key" warning did NOT mean prompts fell back off Prompt Hub. prompt_service.py:32 builds its own Langfuse client from Settings (which reads .env via the YTFLOW_ prefix), so prompt fetching was always correct; only the implicit tracing client was unconfigured. API is now relaunched with LANGFUSE_* exported (0 warnings at startup) so traces are captured too.

## E2E Iteration 3 — BLOCKED (scenario stage capacity wall)

- **[2026-08-04T15:25:11Z]** Escalating ladder of evidence, 6 run attempts:
  | max_tokens | SCP | run | outcome |
  |---|---|---|---|
  | 16384 (.env) | SCP-999 | 2f362fac / 118d5c37 / ae0f6735 | scenario/**structure** truncated, 3/3 deterministic |
  | 16384 | SCP-049 | 198e498e | scenario/**structure** truncated -> NOT SCP-specific |
  | 32768 | SCP-049 | 81bf9f91 | structure **PASSED**, scenario/**writing** truncated |
  | 65536 | SCP-049 | 6cb610c7 | writing did not truncate, but the call has been outstanding **29+ min**; API at 0.3% CPU with an open HTTPS conn to DeepSeek, 0 artifacts written |
- **[2026-08-04T15:25:11Z]** CONCLUSION — this overturns the Story 6.9 assumption for these stages: the 16k truncation was NOT runaway generation, it was a genuine budget shortfall (structure passed cleanly the moment the budget doubled). The real defect is that scenario/writing is a SINGLE DeepSeek call for ALL 8-12 scenes of Korean narration — grep confirms writing_step has no batching (no BATCH/batch_size/chunk anywhere in scenario_chain.py). At the budget it actually needs, one call takes 30+ minutes, which is not viable as a pipeline stage. The production scenario/structure prompt also now demands more per scene than when baseline c6be1954 ran on 2026-07-12 (5.17's title/kicker card fields were promoted to production since), which is what pushed structure past 16k.
- **[2026-08-04T15:25:11Z]** NEEDS ITS OWN STORY (all beyond this orchestration's authorized scope): (1) batch scenario/writing per scene or per small group; (2) raise the shipped max_tokens default off 8192/16384 with measured headroom; (3) extend Story 6.9's TruncationError -> full-rewrite fallback to cover the initial structure/writing generations, not just the repair path; (4) fix the truncation dump writing a **0-byte** file while logging "full runaway raw -> <path>" — it destroyed the primary diagnostic evidence on every failure.
- **[2026-08-04T15:25:11Z]** LOOSE ENDS FOR JAY: run 6cb610c7 is still outstanding against a detached uvicorn on :8000 launched with YTFLOW_DEEPSEEK_MAX_TOKENS=65536 (an env override, NOT committed to .env). ComfyUI is running detached on :8188. Kill both when done. Runs caf09fc8/2f362fac/118d5c37/ae0f6735/198e498e/81bf9f91 are failed/abandoned rows.
- **[2026-08-04T15:25:11Z]** Jay's "new SCP" requirement was NOT satisfied: SCP-999 is blocked by the structure runaway and SCP-682 (the only other unused SCP) has the broken no-alpha standing_front card 8.20 found.
- **[2026-08-05T11:00:15Z]** RESUMED by Jay ("계속"): run 6cb610c7 confirmed dead (detached uvicorn exited, 0 artifacts). Proceeding to fix the actual blocker — batch scenario/writing per scene.
- **[2026-08-05T11:36:10Z]** SCENARIO BLOCKER FIXED, committed f7639b2. writing_step now batches one DeepSeek call per scene, position is the sole source of scene_num, neighbour context passed via the scene_structure variable only (no Langfuse prompt version move needed). deepseek_max_tokens default 8192 -> 32768. reroll_on_truncation added for structure (whole) and writing (only the truncated scene).
- **[2026-08-05T11:36:10Z]** ROOT CAUSE of the "truncation", finally identified: DeepSeek is a reasoning model, so a length-truncated response returns content == "" with the entire budget consumed by discarded reasoning_content. That is ALSO why every truncation dump was 0 bytes — it was never a reporting bug. Fixed at source; dump now always writes a completion_tokens=/raw_chars= header.
- **[2026-08-05T11:36:10Z]** Incidental but important: the stub cassettes were mutually inconsistent (structure 2 scenes, writing 1), so the stub E2E had only ever exercised ONE scene. Now aligned; stub does 2 scenes of real whisperx/ffmpeg work, and _drain_bg_tasks was raised 10s->60s to pay for the flake that made worse.
- **[2026-08-05T11:36:10Z]** Orchestrator verification: 1954 passed / 1 skipped / 0 failed, ruff clean — the session's claim reproduced exactly. Carried over (out of scope): scripts/eval_prompts.py _RISKY_DEFAULT_MAX_TOKENS=8192 no longer matches the shipped default (harmless, 32768 clears its floor).
- **[2026-08-05T11:55:10Z]** E2E retry on SCP-999 (run ce0a455a) got MUCH further with f7639b2: scenario/structure PASSED, scenario/writing PASSED (batched), visual_breakdown PASSED — then failed at **scenario/cast_decision** truncated (finish_reason=length) at ~1060s. Diagnosis: cast_decision_step is ALREADY per-scene (signature takes a single scene + sentences), so batching is not the gap — it simply is NOT wrapped in the new reroll_on_truncation, which f7639b2 only applied to structure and writing. Contributing factor worth owning: this session's own commits grew that prompt ~30 lines (8.19's controlled role catalog + 8.20's pose_guide_key contract), making it likelier to exhaust reasoning tokens.
- **[2026-08-05T12:20:32Z]** Truncation reroll unified across all 8 scenario stages via _call_stage_with_retry, committed f25c7a3 (structure=whole reroll, writing=truncated scene only, no double-reroll, 2nd truncation propagates, non-truncation errors unaffected). 1964 passed/1 skipped, ruff clean. Retrying E2E.
- **[2026-08-05T12:46:59Z]** Orchestrator idling deliberately. Story work for this run is complete (8.19 done, 11.5 done, 8.20 partial+blocked). What remains: E2E run 370666ba in flight with a state-change watcher armed, and 8.20's technique decision which is Jay's. Marker removed so the Stop hook stops forcing empty turns — Jay raised token/time waste explicitly. Run and watcher are NOT stopped; they continue.
- **[2026-08-05T12:46:59Z]** Baseline for the pending diff captured: c6be1954 (SCP-049) video.mp4 490.689s / 171,035,128 bytes, 155 shot PNGs all 155 distinct by content hash, 9 chapter cards, 10 audio files. NOTE the baseline predates 8.17's plates (created 2026-08-02) so STOCK reuse never ran in it — the plate gate 76267bb PREVENTS a 155->41 regression rather than improving on baseline; do not report it as an improvement.
- **[2026-08-05T12:52:18Z]** E2E run 370666ba failed at **scenario/review** truncated. review_step DOES go through the new reroll, so it truncated twice and propagated as designed — one reroll is not enough. DECISIVE EVIDENCE, now capturable thanks to f7639b2's dump fix: tmp/truncations/scenario_review-1785934212501784484.txt is 121,607 bytes with header `completion_tokens=32765 raw_chars=99629` — the ENTIRE 32768 budget was spent on 99,629 chars of pure reasoning ("Need inspect... Need check... Need analyze..."), never emitting content. Confirms the reasoning-exhaustion model exactly.
- **[2026-08-05T12:52:18Z]** PATTERN CONFIRMED: scenario stages that process ALL scenes in ONE call truncate (structure, writing, review, and critic_agent next by the same shape); stages already batched per scene pass (visual_breakdown, cast_decision, writing-after-f7639b2). review is NOT optional — overall_pass drives the _retry_scope repair loop. Fixing review + critic per-scene, same pattern as writing.
- **[2026-08-05T13:03:17Z]** review+critic batched per scene, committed 53e5c8f (targeted tests 380 passed in 0.72s — full-suite-per-fix abandoned per Jay's efficiency mandate; full suite runs once at the end). Carried over: _format_scene_feedback reads feedback/note/description while the critic prompt emits issue/suggestion, so repair briefs have empty detail (pre-existing, now pinned by a test). Launching E2E attempt.
- **[2026-08-05T13:37:09Z]** Blocker 5 (different class — not truncation): run 64b6d9a8 failed at scenario/visual_breakdown YAML parse, 'found character ` ... line 3'. _yaml_text de-fenced with re.match (anchored at pos 0) so a fence preceded by a markdown heading was never stripped. Fixed position-independently, Story 6.11 problem_mark alignment preserved with a pin test. Committed db2e813, 386 passed in 0.95s.
- **[2026-08-05T14:41:16Z]** Blocker 6 (again a new class): run 23ce9a6a died because visual_breakdown returned a CONVERSATIONAL reply as content (finish_reason != length, so not truncation; reasoning_content fallback correctly not involved — my earlier hypothesis that the fallback leaked was WRONG and is retracted). _parse_with_retry gave YAML syntax failures only the deterministic 6.11 repair and then propagated by design, so 'model ignored the output contract' had no recovery path. Now falls through to the existing one-shot corrective retry, still max 1 corrective LLM call. Committed 54d2898, 388 passed in 0.75s.
- **[2026-08-06T10:36:58Z]** Run 6ed449ee was lost when the previous Claude Code process exited — both uvicorn and ComfyUI died with it despite setsid, and the row is stranded as 'running'. Services restarted; new run 6ab87821-23d3-403b-891a-4f79724b3714 launched with all 6 blocker fixes live.
- **[2026-08-06T10:38:36Z]** Services moved to systemd --user transient units (ytflow-comfy, ytflow-api) because setsid did NOT survive the previous Claude Code process exit and cost run 6ed449ee. Linger=yes, so these outlive any Claude session. Run bad091eb-874e-49c5-bc18-9cfe6915a497 launched under them. Restart cost ~2min of a just-started run; protects the ~3h remaining.
- **[2026-08-06T11:50:59Z]** Blocker 7: run bad091eb cleared structure/writing/visual_breakdown/review/critic/cast_decision (real progress — all prior failure points passed) and died at scenario/tts_normalize (completion_tokens=32768 raw_chars=77634). Enumerated ALL stages to stop patching one at a time: tts_normalize was the LAST whole-script one. Batched per scene, committed bbac414, 392 passed. The whole-script truncation class is now closed.
- **[2026-08-06T12:36:50Z]** 🎉 SCENARIO STAGE PASSED for the first time this session (run fdd69699, reasoning=low). Sanity check: 9 scenes, 68 shots, 0 empty narrations, varied moods, 5.17 chapter titles present -> gate AUTO-APPROVED per Jay's rule. The reasoning-depth fix (a9d10ac) is what unblocked it; 4 rounds of per-scene batching had not been enough.
- **[2026-08-06T13:11:18Z]** Blocker 8 — image stage. NOT a ComfyUI fault: history shows 28/28 prompts succeeded, 0 errors, ~20s each, queue draining normally. The API log shows "ComfyUI health check failed after 1/68 shots, waiting for recovery". check_health (comfyui_client.py:73) uses httpx.Timeout(5.0), but ComfyUI is single-threaded on the GPU — while executing a prompt, GET /system_stats cannot answer within 5s. So a healthy-but-BUSY server is misread as crashed: the node stopped submitting and entered crash-recovery, the already-submitted prompt (8e4c123e) aged in the pending queue past its 900s budget, and the run failed with a misleading "produced no image within timeout". Same false negative made ME SIGKILL a healthy ComfyUI earlier in this session after a curl -m 20 returned 000.
- **[2026-08-06T14:12:57Z]** Run 88c39a7f scenario PASSED (2nd time). Sanity: 9 scenes / 51 shots, 0 empty narration, 0 shots on the generic location fallback, location_key resolved (containment-chamber), moods clinical/dread/revelation, titles 9/9. Gate auto-approved. (Orchestrator false alarm corrected: 'missing location' on the final SceneState is expected — location is a structure-stage field consumed by visual_breakdown, not carried into SceneState.)
- **[2026-08-06T14:49:22Z]** Blocker 10 diagnosis (run 88c39a7f, image 1/51). NOT the health check (verified live: check_health returns HEALTHY in 0.1s against a busy ComfyUI, so b14770a works). Timing is decisive: gate approved 14:12:57Z, first timeout logged 14:28:17Z = 920s = exactly the 900s generation budget, so shot 2's prompt sat QUEUED the whole time. Prompt 41b2d61d is in ComfyUI history with status=success and a valid images output — ComfyUI produced it AFTER the client gave up. History shows 70 IMAGE prompts vs 2 DEPTH, i.e. the backlog was leftover image prompts from earlier failed runs whose ComfyUI queue entries survived the API restarts. ORCHESTRATOR PROCESS ERROR: I cleared the ComfyUI queue before cd2f1fb8 but NOT before 88c39a7f. Also note the log line 'health check failed ... waiting for recovery' is misattributed — _wait_for_comfyui_recovery emits it even when the trigger was a submit_and_fetch timeout (image.py:425), which sent me chasing the health check. Retrying with a cleared queue + restarted API.
- **[2026-08-06T15:09:02Z]** Run 6a9a49d9 scenario PASSED (3rd consecutive). Sanity: 10 scenes / 70 shots, 0 empty narration, 0 generic-fallback prompts, 0 empty image_prompts, moods clinical/dread/escalation/revelation, titles 10/10. Gate approved with a CLEAN ComfyUI queue (0 depth) this time.
- **[2026-08-07T15:19:00Z]** Run 6a9a49d9 was KILLED, not failed: both systemd units were explicitly Stopped at 00:16:27 (Result=success, API SIGTERM 143) while ComfyUI was mid-generation ('got prompt' at 00:14:33). So systemd-run --user did NOT survive Claude session teardown either, same as setsid. png stuck at 1/70. Relaunched units with Restart=always/RestartSec=5 and started run ee8cd319-8e34-4339-98e1-7fd373d08394 with a cleared queue. NOTE: there is no resume path — memory records /stages/{s}/retry wipes rather than resumes and resume_run_from_failure has no route — so every teardown costs the entire run.
- **[2026-08-07T16:18:58Z]** ROOT CAUSE of every SCP-999 image failure at png=1: **missing character assets, not a code defect**. Log: 'generate_special_pose_card: no standing front card for SCP-999'. DB confirms character_cards has approved rows ONLY for SCP-049 (front/back/side/three_quarter) + STOCK-d-class; SCP-999 has zero. So the image stage kept attempting slow character-card generation, which filled the ComfyUI queue and starved the shot prompts until they hit the 900s budget. The queue-leftover hypothesis was DISPROVEN (this run started with a cleared queue and still grew to 8). Switching to SCP-049: it has full approved cards AND is baseline c6be1954's SCP, making the requested diff apples-to-apples. Jay's 'new SCP' requirement remains unmet and now has a concrete cause + fix path (pre-generate cards for the new SCP).
- **[2026-08-07T16:38:28Z]** Run 8a9a288b (SCP-049) scenario PASSED. Sanity: 9 scenes / 66 shots, 0 empty narration, 0 generic-fallback prompts, 0 empty image_prompts, 51 shots with cast (8.19's guard active), moods clinical/dread/escalation/revelation, titles 9/9. Gate approved. This SCP has full approved cards so the image stage should not starve on card generation.
- **[2026-08-07T17:28:46Z]** 🔴 REGRESSION I INTRODUCED (624dd2f): resuming run 8a9a288b marked it **status=complete with NO video, 1 png, gate_states image=failed, error=none** — a FALSE COMPLETE. Root cause: image_node follows the AD-10 pattern and catches its own exception, returning a state dict with `error` set instead of raising. So LangGraph records that node as SUCCESSFUL, and resume_run_from_failure's `astream(None, config)` replays PAST the failed stage instead of re-running it. The pre-existing service function's docstring premise ('a run that failed after scenario resumes at image') is wrong for any node using the AD-10 swallow pattern — which is all of them. Exposing it via a route made the false-complete reachable. Must rewind to the failed stage (retry_stage semantics) rather than replay forward.
- **[2026-08-07T18:05:26Z]** 🎯 IMAGE-STAGE ROOT CAUSE FOUND, first run after the observability fix (3576e1d): ComfyUI returns prompt status_str=success, completed=True, **outputs=[]**, with messages [execution_start, **execution_cached**, execution_success]. This is ComfyUI's EXECUTION CACHE: an identical workflow re-submission hits the cache, SaveImage never re-runs, and the response carries NO outputs. The client polls for 'images', never finds any, burns the full budget and reports the misleading 'produced no image within timeout'. Four earlier hypotheses (queue leftovers, missing cards, health-check false negative, ComfyUI restart) were all disproven; this one is confirmed by ComfyUI's own message stream. Fix: the client must treat an execution_cached/no-output response as 'reuse the prior output' rather than waiting forever.
- **[2026-08-07T18:48:34Z]** After b36aaa0 the error became precise: 'the prompt never appeared in history (900 polls, 0 errored)' — network perfect, prompt never queued. Verified by hand that submission works: the cache-busted workflow posts HTTP 200 with node_errors={} and the prompt completes in 20s with output node 9. LIKELY MY OWN INTERFERENCE: I repeatedly called /queue clear and /interrupt while diagnosing; a clear landing just after the run submits discards the prompt and produces exactly this signature. Retrying WITHOUT touching the ComfyUI queue.
- **[2026-08-07T20:33:28Z]** Image stage reached **png=5/66** — first time past shot 2 all session (b36aaa0's execution_cached fix was real). Then failed again with 'the prompt never appeared in history (900 polls, 0 errored)' WITHOUT any queue interference from me, so my interference hypothesis is disproven too. Conclusion: ComfyUI intermittently returns HTTP 200 + prompt_id but never queues/executes the prompt (dropped after 1, 2, and 5 successful shots on different attempts). Correct client-side response regardless of ComfyUI's internal reason: detect 'in neither /queue nor /history' and resubmit, instead of polling 900s for a prompt that was never accepted.
- **[2026-08-07T21:56:00Z]** Verified STALLED (not slow): 90s window gave png +0, history +0, zero ComfyUI executions in 5 min, drop-recovery never fired. ComfyUI held prompt d451140e 'running' at 83.5% CPU / 47.6% MEM with a 6h36m-old process and no log output — genuinely wedged after hours of accumulated model state, unlike the earlier busy-but-healthy case I misdiagnosed. Restarted both units fresh and re-ran the image stage.
- **[2026-08-07T23:23:32Z]** BOTTLENECK MEASURED: a depth cache MISS takes **>10 minutes** (probe timed out at 600s incomplete). ComfyUI generation itself is healthy at 20-24s; ComfyUI sat IDLE most of the time (5-6 executions in 25 min) — the image stage was STARVED, not slow. Story 11.5 computes depth per shot synchronously in the critical path; the 42 approved plates were fast only because they were cache hits, while every generated shot background is a fresh inference. 66 shots x ~10 min = ~11 hours. Jay chose: disable depth for this run and finish. API restarted with YTFLOW_DEPTH_PLACEMENT_ENABLED=false (env override only, NOT committed — config default stays True). This run's video will therefore use zoompan fallback instead of 11.5 parallax; 11.5 keeps its own live-gate evidence and A/B clips at ~/ytflow-11-5-review/ for separate evaluation. The depth performance defect needs its own story.

## E2E BLOCKED ON HOST RESOURCES (not a yt.flow defect)

- **[2026-08-08T00:58:39Z]** Image generation degraded from **16.1s -> 336.6s -> 900s+** per shot for the IDENTICAL workflow (1344x768, 30 steps, dpmpp_2m/karras), measured by hand on the same ComfyUI instance. ComfyUI process sits at ~101% CPU with 16GB RSS; host RAM is 31GB total / 20GB used / **0GB free**; ComfyUI runs in lowvram mode (`lowvram patches: 667`, `Unloaded partially: 2332MB`) offloading weights to a system RAM that has nothing left, so it thrashes and gets progressively worse. POST /free {"unload_models":true,"free_memory":true} returned 200 but freed nothing (a generation is in flight). Restarting ComfyUI did NOT fix it (fresh instance re-reached 16GB in ~29 min).
- **[2026-08-08T00:58:39Z]** TWO ORCHESTRATOR MISDIAGNOSES CORRECTED: (1) "depth is the bottleneck" was WRONG — DepthAnythingV2Preprocessor prompts complete in **0.7s**; the 400s prompts are the base SDXL generation. Jay disabled depth on that false basis and can turn it back on. (2) "restarting ComfyUI clears lowvram" was wrong — it recurs.
- Run 8a9a288b state: scenario approved, image **10/66**, still running but ~6-15 min per shot and degrading. Artifacts verified genuine: 10 distinct images by sha256, ~1.2MB each.
- NEEDS JAY: free host RAM (or reboot), or launch ComfyUI with VRAM-resident flags so it stops offloading to a full system RAM. Then `POST /runs/8a9a288b-800f-4c73-88a2-25ae6b5a4d7d/resume` — the 10 completed shots are preserved by _existing_complete_shot.
