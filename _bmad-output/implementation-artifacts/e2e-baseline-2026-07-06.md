# E2E Baseline — Iteration 0 (2026-07-06)

목적: Epic 1~7 전체가 켜진 상태의 **첫 통합 렌더**를 실제 UI(Playwright)로 뽑고, 사전에 고정한 루브릭으로 품질을 측정해 자가 개선 루프(iteration 1+)의 기준점을 만든다. **이 run의 목적은 측정이지 완벽한 영상이 아니다** — 결함은 고치지 않고 기록한다.

## 환경

- 코드: master `eb9e296` (working tree clean)
- 피험자: **SCP-049** — 캐릭터 레코드 없음 → 5-10 자동 프로비저닝 전체 경로(위키 fetch → IPAdapter 멀티앵글 → 앵글 선택) 최초 발동
- 활성 기능: layered(InSPyReNet)+인페인트(5-2/5-6/5-7), 모션(5-3/7-3), TTS 정규화(5-4), 사운드 디자인(7-1, CC0 12파일), 컬러 그레이드(7-2), 전환 다양화(5-1/7-4), 키네틱 자막(7-5), 오디오 연속성(5-9)
- 서비스: FastAPI :8000, ComfyUI :8188 (ROCm), Langfuse OK, DeepSeek v4-flash, Qwen TTS(voice clone)

## 절차

1. UI(`/app`)에서 SCP 피커로 run 생성 — API 직접 호출 금지, 실사용자 시뮬레이션
2. 각 게이트에서 아티팩트를 **실제로 읽고/보고** 아래 루브릭으로 판정 후 승인/재시도
3. 최종 영상: ffprobe 결정적 체크 + 프레임 샘플링(씬 경계 ±0.5s, 씬 중앙) 시각 판정
4. 리포트(이 파일에 추가) + 결함 백로그 작성

## 게이트별 결정적 체크 (하나라도 실패 → 원인 확인 후 재시도, 스테이지당 최대 2회)

| 게이트 | 체크 |
|---|---|
| scenario | 씬/샷 구조 존재, 씬별 `mood` ∈ {dread, clinical, escalation, revelation}, 샷↔나레이션 문장 1:1, image_prompt 비어있지 않음 |
| image | 샷 수만큼 파일 존재, 레이어드 샷은 배경(불투명)+캐릭터(RGBA) 쌍, 0바이트/플레이스홀더(≤1KB) 없음 |
| tts | 샷 수만큼 오디오, 전부 길이 > 0.5s, 총 길이 2~8분 범위 |
| subtitle | .ass 존재, 이벤트 수 ≈ 문장 수, 키네틱 태그(\t, \fad 등) 포함 |
| video | h264+aac 스트림, 총 길이 ≈ Σ나레이션+전환+챕터카드, 오디오 무음 아님(라우드니스 측정치 기록) |

## 판정 루브릭 (각 축 1~5, 근거 필수 — Jay의 2026-07-03 피드백 5건이 시드)

| 축 | 내용 | 근원 |
|---|---|---|
| J1 전환 | 오버랩 없음, fadeblack/다양화 전환, 챕터 카드, 오디오 연속(볼륨 패임 없음) | 피드백#1, 5-1/7-4/5-9 |
| J2 역동성 | 줌/팬 체감, 캐릭터 패럴랙스·아이들 모션 | 피드백#2, 5-3/7-3/1-9c |
| J3 레이어드 합성 | 캐릭터 오버레이 실재, 이중노출 없음, 컷아웃 경계 깨끗 | 피드백#3, 5-2/5-6/5-7 |
| J4 비주얼-스토리 정합 | 개체 외형이 SCP-049 정체성(페스트 닥터)과 일치, 샷별 이미지↔나레이션 정합, 앵글 다양성 | 피드백#4, 5-5/5-10/5-12/5-13 |
| J5 TTS 한국어 자연화 | 오독 유발 표현 없음(텍스트 기준), 문장 리듬 | 피드백#5, 5-4 |
| J6 사운드 디자인 | mood 배정 타당, BGM/앰비언트/스팅어 실재+믹스 레벨(측정) | 7-1 |
| J7 후처리 | mood별 컬러 그레이드 식별 가능, 과하지 않음 | 7-2 |
| J8 자막 | 키네틱 스타일 적용, 가독성(그레이드와 간섭 없음) | 7-5 |

**한계 명시**: J5 억양·J6 음악 취향의 청각 판정은 AI가 못 함 → 텍스트/측정치 기반 프록시로 채점하고, 최종 청취 판정은 Jay 몫으로 남긴다. 판정-실청취 간 불일치가 나오면 루브릭을 수정한다(루프의 일부).

## 의사결정 규칙

- **PASS**: 결정적 체크 전부 통과 → 승인. 판정 축 3점 미만이어도 **베이스라인에서는 승인하고 결함 기록** (후속 스테이지를 무의미하게 만드는 수준 — 예: 이미지 전면 깨짐 — 만 재시도)
- **RETRY**: 결정적 체크 실패 시. 같은 스테이지 2회 실패 → ABORT
- **ABORT**: 파이프라인 결함으로 진행 불가 → 결함 리포트 후 종료 (수정은 스토리로, 이 세션에서 즉흥 수정 금지)

## 산출물

- 이 파일 하단에 리포트: 게이트별 판정 로그, J1~J8 점수+근거, 결함 백로그(스토리 후보), Jay 확인 필요 항목

---

# 리포트 (run `272b05a4`, SCP-049)

## scenario 게이트 — 승인 (결함 3건 기록)

- 구조: 8씬 59샷, 샷↔문장 1:1 매핑 정상, image_prompt 공백 없음, 카메라 앵글 8종 분산 양호 (close-up 13, medium 11, wide 9, POV 7...)
- **[결함 D1 — major] mood 택소노미 위반**: 8씬 전부 writing LLM이 자유형 mood 출력("shock", "mystery", "intrigue", "horror", "awe mixed with dread", "tension", "unresolved mystery"; 유효값은 scene8 "dread" 1건). 원인: `structure.md`는 enum을 강제하지만 최종 mood는 `scenario_chain.py:372`가 **writing 출력**에서 읽음 — writing 프롬프트에 enum 제약 없음. `resolve_mood` 폴백으로 크래시는 없으나 **전 씬이 dread로 수렴 → 7-1 BGM/7-2 그레이드/7-4 전환 다양성이 이 run에서 관찰 불가**. 수정 후보: mood를 structure 출력에서 읽거나(배선), 체인에서 resolve_mood 정규화, 또는 writing 프롬프트에 enum 제약(candidate 라벨로).
- **[결함 D2 — minor] 아티팩트 API가 mood 미노출**: scene 직렬화에 mood 키 없음 → 게이트에서 사람이 mood를 검수할 수 없음.
- **[결함 D3 — minor] 시각 디스크립터 없는 리터럴 SCP 토큰**: 6/59 프롬프트가 "SCP-049(-2)"를 외형 묘사 없이 사용 — SDXL엔 무의미 토큰. 특히 SCP-049-2(좀비 개체)는 캐릭터 레코드도 없어 보정 경로 부재.
- J5(TTS 자연화) 예비 4/5: "에스시피 공사 구", "키 일점 구 미터", "디 계급" 등 5-4 정규화 실제 작동. "마이너스 이"(049-2) 다소 어색, "소름" 2회 반복 — 경미.
- J4(프롬프트 수준) 예비 4/5: 개체 디스크립터(plague/beak/porcelain) 31/59, 씬 맥락 정합 양호.
- **[결함 D4 — minor] SPA 딥링크 404**: `/app/runs/{id}` 직접 진입 시 `{"detail":"Not Found"}` — static mount에 SPA fallback 없음. 대시보드 경유로만 접근 가능.

## 캐릭터 프로비저닝 (5-10/5-12/5-13 경로) — 정상 작동 확인

- 위키 fetch: 공식 SCP-049 사진 확보(`workspace/SCP-049/references/ref_1.png`) → Qwen-VL 디스크립터 실제 생성 → IPAdapter 4앵글 생성, 4장 전부 상이.
- 정체성 유지 탁월: 페스트 마스크·검은 로브·사슬까지 레퍼런스 충실, 애니메 스타일 변환 자연스러움.
- **[결함 D5 — moderate] 앵글 라벨 불일치**: "side"가 프로필이 아닌 준정면, "back"도 정면 구도(부리 이중 왜곡). IPAdapter 정면 레퍼런스가 앵글 프롬프트를 지배 — 1.13 앵글 선택이 라벨 기준으로 고르므로 다양성 실효 저하. 수정 후보: 앵글별 IPAdapter weight 하향 또는 앵글 프롬프트 강화.

## image 스테이지 1차 실행 — 인프라 크래시로 실패 (39/59샷 지점, 재시도 1회차 진행)

- **[결함 D6 — infra/major] ROCm 드라이버 크래시**: 78개 이미지(~39샷) 생성 후 ComfyUI가 `hipErrorIllegalAddress`(illegal memory access)로 core dump. yt.flow 코드 결함 아님 — RX 9060 XT ROCm의 지속 부하 불안정. 파이프라인은 계약대로 행동(5-11 플랫 폴백 시도 → ComfyUI 사망으로 불가 → 명확한 에러 메시지로 run 실패). 완화 후보: ComfyUI 감시/자동재기동, VRAM 압력 완화 옵션, 파이프라인 측 ComfyUI 헬스체크+대기 재시도.
- **[결함 D7 — minor] 스테이지 실행 중 상태 표기 랙**: image 노드가 실제 생성 중인 동안 `/runs/{id}`가 `awaiting_approval`/`current_stage=scenario`를 계속 반환 — UI가 장시간 스테이지 동안 "승인 대기"로 오표기.
- **[결함 D8 — moderate] 이미지 스테이지 샷 단위 재개 불가**: `image_node`에 기존 파일 스킵 로직 없음 → 재시도 시 59샷 전량 재생성. 이번 크래시로 78장(약 30~40분 GPU 시간) 낭비. 수정 후보: 샷별 산출물 존재+무결성 확인 시 스킵.
- **[결함 D9 — major/UI] 실패 스테이지에서 재시도 버튼 무동작**: run failed 상태에서 image 패널의 "재시도" 클릭이 네트워크 요청을 전혀 발생시키지 않음(아티팩트 GET 404 상태에서 핸들러 무반응 추정). 베이스라인 진행 위해 이 액션만 `POST /stages/image/retry` API로 우회(202 수신, 재시도 정상 가동).

## image 게이트 — 승인 (재시도 2회차 크래시 없이 완주, 결함 2건 기록)

- 결정적 체크 전부 통과: 59샷 × (main+bg+RGBA char) 177파일, 폴백 0, 파손/미니파일 0. D7 정정: 재시도 경로에서는 status가 "running image"로 정상 표기 — 랙은 게이트 승인 후 재개 경로 한정.
- 잘 되는 것: 단일 인물 샷 컷아웃 우수(S00701 마스크 클로즈업 등), **이중노출은 전 표본에서 해소** (5-7 유효), 정체성 유지 일관.
- **[결함 D10 — moderate] 인페인트 흉터**: 개체 제거 자리에 환각 구조물(S00401 갈색 더미), 실루엣 구멍(S00301), 벽 얼룩. 배경 단독으로 보면 눈에 띔 — 오버레이가 가려주는 샷도 있으나 카메라 무빙 시 노출.
- **[결함 D11 — major/설계] 환경 샷에도 무조건 레이어드 적용**: visual_breakdown에 샷별 개체 등장 플래그가 없어 image_node가 전 샷 세그멘테이션 수행 → 인물 없는 샷에서 방 전체(S00100)·빛 얼룩(S00604 흰 스미어)을 "캐릭터"로 절취. 오버레이 시 유령 얼룩/방 전체 흔들림 예상. 수정 후보: 시나리오 단계에서 `has_entity` 플래그 → 해당 샷만 레이어드, 나머지는 플랫 경로.
- J3 예비 2/5, J4(이미지 수준) 예비 3/5 — 최종 확정은 video 프레임 판정에서.

### Jay 실시간 피드백 (2026-07-06, image 게이트 시점) — 아키텍처 결정

> "뭔가 어긋나있는 느낌. 시공간이 찢어져서 공존하는 느낌. 기존 이미지에서 개체를 억지로 칼로 베어낸 느낌."

D10/D11과 동일 진단. **합성 방식 결정 확정**: 배경은 배경 묘사만으로 생성(개체 미포함) + 캐릭터는 레퍼런스 기반 별도 생성 이미지(5-10 앵글) 사용 → 합성. 캐릭터 불필요 씬은 배경만. 즉 **동일-프레임 세그멘테이션+인페인트 방식(1.6b/5-6/5-7 계열) 폐기** 방향. 필요 작업: ① visual_breakdown에 샷별 has_entity + 개체 제거된 배경 프롬프트, ② image_node에서 세그/인페인트 경로 제거(배경 생성만), ③ 오버레이 소스를 캐릭터 앵글 이미지로 일원화(1.13 경로 기본화). 부수 효과: 5-6 컷아웃 품질·5-7 이중노출 문제 계급적 소멸.

**추가 결정 (Jay)**: SCP 세계관 고정 출연진(D계급, 연구원/박사, 경비요원 등)을 **캐릭터 카드로 사전 생성·캐싱해 재사용**. 설계 메모: `CharacterModel`이 이미 scp_id 키 + 4앵글 캐시 구조라 `STOCK-*` 예약 키로 시드하면 스키마 변경 불요; has_entity 플래그를 불리언 대신 샷별 `cast` 목록으로 설계하면 고정 캐릭터·다중 오버레이(개체+요원 동시 등장)·파생 개체(049-2) 카드화까지 한 구조로 해결; 에피소드 간 시각 일관성(채널 아이덴티티) 부수 효과.

**확장 확정 (Jay)**: 배경 + 캐릭터 카드 N장 합성. 요구사항: 샷별 cast 항목에 **배치 메타데이터**(position/scale/z-order — 대략적 좌우·원근 수준) 필수, 없으면 카드가 중앙에 겹침. 이점: 카드별 독립 아이들 모션/패럴랙스(1.9c/7-3)로 다층 생동감, 구도가 SDXL 프롬프트 의존에서 visual_breakdown 데이터 제어로 이동.

## tts / subtitle 게이트 — 승인

- tts: 8씬 21.6~32.0s, 총 225.6s(3:46), 볼륨 -22.6~-24.9dB 균일 — 전부 통과.
- subtitle: 8개 .ass, 가라오케 `{\k}` 키네틱 태그 실재(7-5), Noto Sans CJK 48pt 스타일 정상.
- **[결함 D12 — known] whisperx 단어 정렬 전 씬 폴백**: 8씬 모두 씬별 균일 k값(55~70cs) — 단어 타이밍이 usable하지 않아 eb9e296의 세그먼트 폴백이 100% 발동. 가라오케 싱크는 근사치. 기존 spec(`spec-subtitle-word-segment-fallback.md`) 소관, 근본 원인(한국어 정렬 모델) 조사 필요.
- **[결함 D14 — minor/UI] 자막 패널 카운트 오류**: 내용은 렌더되는데 "자막 0개" 표기.

## video 게이트 — 승인, run **complete** (전 게이트 승인)

- 결정적 체크 통과: 3:51(230.9s ≈ 나레이션 225.6 + 카드 - xfade 오버랩), 1080p/25fps h264+aac, 통합 라우드니스 -23.6 LUFS, 카드 구간에도 오디오 베드 실재(-25.4dB — 7-1 사운드 디자인 작동).
- 전환: **fadeblack + 챕터 카드 정상, 오버랩 없음** (2026-07-03 피드백 #1 해소 확인). Ken Burns 미세 줌 확인.
- 챕터 카드는 검은 화면에 "- N -" 숫자만 — 제목/타이포 없음 (개선 후보, 스토리 미채택).
- **[결함 D13 — critical] 전 샷 동일 캐릭터 카드 풀프레임 오버레이**: 12/12 샘플 + 16 버스트 프레임 전부에서 동일한 front 앵글 카드가 화면을 지배 — 배경 다양성(59샷 각기 다른 프롬프트)이 전부 가려져 "4분 내내 같은 그림". **근본 원인 확정**: 5-10의 앵글 카드가 **RGB 무알파 1664×928 풀프레임 일러스트**인데, video_node 합성 경로는 투명 RGBA 스프라이트를 전제(video.py:304) — 1.13 앵글 선택이 전 샷의 character_path를 이 불투명 카드로 덮어써서 발생. 계약 불일치 2건: ① 알파 없음 ② 스프라이트가 아닌 풀신 프레이밍. D5(앵글 전부 준정면)와 결합해 다양성 0.

## 최종 점수 (J1~J8)

| 축 | 점수 | 근거 |
|---|---|---|
| J1 전환 | **4/5** | fadeblack+카드+오디오 연속 작동, 오버랩 소멸. 감점: 카드 민짜, 7-4 다양화는 D1로 관찰 불가 |
| J2 역동성 | **2/5** | Ken Burns 확인되나 D13 거대 정적 오버레이가 체감 지배 |
| J3 합성 | **2/5** | 이중노출 해소(+). D10 인페인트 흉터, D11 환경샷 오컷, D13 |
| J4 정합 | **2/5** | 프롬프트·배경 정합은 양호했으나 최종 화면이 전 샷 동일 → 나레이션(수술/훈련/시신)과 불일치. 개체 정체성 자체는 5/5 |
| J5 TTS | **4/5** (텍스트 프록시) | 5-4 정규화 우수("에스시피 공사 구" 등). 청취 판정 Jay 몫 |
| J6 사운드 | **3/5** (측정 프록시) | 베드 실재 확인. mood 다양성은 D1로 전부 dread 추정, 믹스 밸런스 청취 필요 |
| J7 후처리 | **관찰 불가** | 전 프레임 동일 톤 — D1 mood 붕괴로 그레이드 다양성 검증 자체가 불가 |
| J8 자막 | **3/5** | 키네틱 렌더+가독성 OK, D12 균일 타이밍 |

## 결론 (iteration 0)

파이프라인 **기계는 완성됨** — 실제 UI로 생성→5게이트→완성까지 통짜로 돌고, Epic 5~7 기능이 전부 실재함. 품질 병목은 명확히 **이미지 합성 아키텍처 1개**로 수렴: D5+D10+D11+D13이 전부 "같은 프레임에서 오려내기 + 카드 계약 불일치"의 파생. Jay 결정대로 배경+카드 컴포지팅으로 전환하는 것이 차기 최우선. 부차 트랙: mood 배선(D1, Epic 7 가치 절반이 여기 묶임), 복원력(D6/D8), UI 수리(D4/D9/D14/D7).

**judge 캘리브레이션 항목 (Jay 시청 후 확인)**: J1 4점(전환)이 실제 체감과 맞는지, J6 믹스 밸런스, J5 억양.
