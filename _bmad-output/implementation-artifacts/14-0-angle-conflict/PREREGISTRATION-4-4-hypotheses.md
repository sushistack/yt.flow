# 사전등록 — §4-4 후보 가설 (a) vs (b) 판별 (2026-08-22)

이 파일은 **이미지를 한 장도 보기 전에** 작성했고 이후 수정하지 않는다.
(`gotcha_a-prompt-derived-question-is-a-leading-question`: 프롬프트에서 도출한 체크리스트는
유도질문이다. 그래서 시점 판정은 프롬프트 텍스트가 아니라 **투영기하 한 개**로만 내린다.)

## 판정 대상
run `4b35c0ed`의 출하 배경 플레이트 43장 = `workspace/<run>/images/scene_*_S00*.png` (1344×768).
`stock_plate_substitution_enabled=False`이므로 43장 전부 `image_prompt`에서 생성됐다 —
프롬프트→시점 대응이 성립하는 유일한 조건.

## 시점 판정 규칙 (프롬프트 비열람, 이미지 단독)
1차 지표는 **소실점/지평선(눈높이 선)의 프레임 세로 위치** `y_h` — 0.0=상단, 1.0=하단.
수평 카메라에서 지평선은 정확히 0.5에 온다(투영기하). 아래로 기울면 위로, 위로 기울면 내려간다.

| 판정 | 규칙 |
|------|------|
| `HIGH` (부감) | `y_h < 0.40`, 또는 지평선이 상단 밖(바닥면이 프레임을 채움) |
| `EYE` (눈높이) | `0.40 ≤ y_h ≤ 0.60` |
| `LOW` (앙각) | `y_h > 0.60`, 또는 천장면이 지배적이고 바닥이 거의 없음 |
| `UNREADABLE` | 판독 가능한 지면/원근 단서 없음 (추상·클로즈업 등) |

판독 보조: 각 타일에 `y=0.40`(빨강)·`0.50`(회색)·`0.60`(파랑) 선을 굽는다.
판정은 "소실점이 빨강 위 / 사이 / 파랑 아래" 3분기로 기계적으로 읽는다.
부수 기록: `ceiling_visible`(Y/N), `floor_share` 버킷(0–25/25–50/50–75/75–100 %).

## 판정 순서(맹검 확보)
`shot_id` 오름차순으로만 본다. 그룹 순서로 정렬하지 않는다.
**축 (i)(바닥/천장 서술 질량)은 43장 판정을 끝낸 뒤에 계산한다** — 판정 시점에 그 값을 모른다.
축 (ii)(조명 어휘 히트)는 `measure_angle_agreement.py`의 감사 출력으로 **이미 알고 있다**
(23건/20샷). 이 비대칭은 보고서에 명시한다: (ii)는 맹검이 아니고 (i)는 맹검이다.

## 축 정의 (판정 전에 고정, 값은 판정 후에 계산)
- **축 (i) 바닥/천장 서술 질량**: `image_prompt` 전체에서 아래 표면 어휘 히트 수, 그리고 100단어당 정규화.
  - 바닥: `floor` `ground` `concrete` `tile|tiled|tiles` `drain` `grate` `linoleum` `carpet` `pavement` `asphalt` `floorboard`
  - 천장: `ceiling`(단, `ceiling-mounted`는 제외) `rafter` `ceiling beam` `ductwork|duct` `skylight`
  - 축 (ii) 3구절(`overhead` / `from above` / `ceiling-mounted`)은 축 (i)에서 **제외** — 두 축이 겹치면 판별이 안 된다.
- **축 (ii) 조명 어휘 히트**: `_LIGHTING_DECOYS` 그대로 (`overhead`, `from above`, `ceiling-mounted`), 프롬프트 전체.

## 사전 예측 (반증 가능하게)
- **(a) 내용 질량이 참**이면: 축 (i) 상위군에서 `HIGH` 비율이 유의하게 높고, 축 (ii)는 판정을 예측하지 못한다.
- **(b) 조명 어휘 오독이 참**이면: 축 (ii) 히트 보유군에서 `HIGH` 비율이 높고, 축 (i)는 예측하지 못한다.
- **두 축이 갈라지는 샷**(고-(i)/저-(ii), 저-(i)/고-(ii))이 결정적이다. §4-4가 지목한 `S00901`·`S00103` 포함.
- **둘 다 예측 못 하면**: 두 가설 모두 기각이고 세 번째 층(예: 슬롯-1 뒤쪽의 카메라 어휘, 체크포인트 사전확률)을 봐야 한다.
